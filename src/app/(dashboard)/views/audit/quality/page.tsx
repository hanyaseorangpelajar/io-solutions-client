import { AuditQualityPage } from "@/features/audit";

export const metadata = {
  title: "Ticket Audit Quality — I/O SOLUTIONS",
};

export default function Page() {
  return <AuditQualityPage />;
}
