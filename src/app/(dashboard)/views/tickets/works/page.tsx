import { TicketsMyWorkPage } from "@/features/tickets";

export const metadata = {
  title: "My Work — I/O SOLUTIONS",
};

export default function Page() {
  return <TicketsMyWorkPage />;
}
