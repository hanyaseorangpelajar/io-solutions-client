import { TicketsHistoryPage } from "@/features/tickets";

export const metadata = {
  title: "Riwayat Tickets — I/O SOLUTIONS",
};

export default function Page() {
  return <TicketsHistoryPage />;
}
